// Backend API base URL.
// On Vercel (production): /api routes to the Express serverless function — same origin, no domain needed.
// For local dev: change to "http://localhost:3000" and run: node backend/src/server.js
window.SAZIN_API_URL = "/api";
